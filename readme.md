# Parallel Postman Stress

## Concurrency
The variable `PARALLEL_RUN_COUNT` may be modified to specify the number of concurrent runs.

## Running
You can run this script by executing `npm start` in the terminal.
